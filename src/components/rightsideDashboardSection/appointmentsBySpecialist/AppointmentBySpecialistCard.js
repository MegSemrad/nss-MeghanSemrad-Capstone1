import React from "react";
import Button from "react-bootstrap";


export const AppointmentBySpecialistCard = ({ ?? }) => {
  return (
    <Button variant="primary" size="lg" block 
    onClick={() => { history.push(`/AppointmentBySpecialist/edit/${??.id}`)>
        {??.speciality}
    </Button>
)}

